session_name: nodex
start_directory: /Users/{{ .chezmoi.username }}/ghq/github.com/{{ .side_work_org }}/{{ .side_work_project }}-platform

options:
  status-left: "[nodex] Ctrl+b w:list | f:follow | ':number | z:zoom | [:scroll | d:detach "
  status-left-length: 80
  status-right: ""
  remain-on-exit: on
  automatic-rename: off

windows:
  # Window 0: Dashboard (launcher + monitor)
  - window_name: dashboard
    layout: main-vertical
    options:
      main-pane-width: 50%
    panes:
      - shell_command:
          - /Users/{{ .chezmoi.username }}/ghq/github.com/riii111/dotfiles/.config/mux/core/mux-launcher nodex
      - shell_command:
          - /Users/{{ .chezmoi.username }}/ghq/github.com/riii111/dotfiles/.config/mux/core/mux-monitor nodex

  - window_name: studio-api
    start_directory: /Users/{{ .chezmoi.username }}/ghq/github.com/{{ .side_work_org }}/{{ .side_work_project }}-platform/backend
    panes:
      - shell_command:
          - echo "=== studio-api ===" && echo "Waiting for start command..."

  - window_name: studio-fe
    start_directory: /Users/{{ .chezmoi.username }}/ghq/github.com/{{ .side_work_org }}/{{ .side_work_project }}-platform/frontend
    panes:
      - shell_command:
          - echo "=== studio-fe ===" && echo "Waiting for start command..."

  - window_name: {{ .side_work_project }}-agent
    start_directory: /Users/{{ .chezmoi.username }}/ghq/github.com/{{ .side_work_org }}/{{ .side_work_project }}-agent
    panes:
      - shell_command:
          - echo "=== {{ .side_work_project }}-agent ===" && echo "Waiting for start command..."

  - window_name: docker
    start_directory: /Users/{{ .chezmoi.username }}/ghq/github.com/{{ .side_work_org }}/{{ .side_work_project }}-platform
    panes:
      - shell_command:
          - echo "=== Docker Services ===" && echo "Waiting for start command..."
