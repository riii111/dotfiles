# Gradle Daemon settings
# Auto-shutdown after 10 minutes idle (default: 3 hours)
org.gradle.daemon.idletimeout=600000

# Kotlin Compile Daemon auto-shutdown after 2 hours idle
kotlin.daemon.jvm.options=-Xmx512m
