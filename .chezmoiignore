# Repo files that shouldn't be in $HOME
Brewfile
README.md
LICENSE
.gitignore

# Personal machine only
{{ if ne .type "personal" }}
.local/bin/nodex
.config/mux/nodex
{{ end }}

# Work machine only
{{ if ne .type "work" }}
.gitconfig-riii111
.claude/skills/deploy-preflight
{{ end }}

# Auto-generated files
.config/karabiner/automatic_backups
.config/karabiner/assets

# Machine-specific (trust levels differ per machine)
.codex/config.toml

# Machine-specific runtime data (not for sharing)
.config/colima
