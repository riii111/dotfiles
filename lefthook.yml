pre-commit:
  commands:
    shell-fast-check:
      stage_fixed: true
      run: ./bin/executable_dotctl lint-staged-shell

post-merge:
  commands:
    chezmoi-apply:
      run: chezmoi apply --force < /dev/null
