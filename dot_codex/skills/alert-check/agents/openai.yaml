interface:
  display_name: "Alert Check"
  short_description: "Triages prod alerts with pe"
  default_prompt: "Use $alert-check to triage production alerts with pe, gh, and gcloud, then report the likely cause and next action."

policy:
  allow_implicit_invocation: true
